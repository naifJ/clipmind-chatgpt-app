services:
  - type: web
    name: clipmind-chatgpt-app
    runtime: node
    rootDir: clipmind-chatgpt-app
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
